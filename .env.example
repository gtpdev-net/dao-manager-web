# Database Test Configuration
# ============================
# Copy this file to .env and fill in your values (DO NOT commit .env to git)
# These environment variables are used by test projects to connect to SQL Server

# Option 1: Full connection string (most flexible)
# Use {database} as a placeholder - it will be replaced with the unique test database name
# TEST_DB_CONNECTION_STRING=Server=localhost,1433;Database={database};User Id=sa;Password=YourSecurePassword;TrustServerCertificate=True;Encrypt=False;

# Option 2: Individual components (simpler for most cases)
TEST_DB_SERVER=localhost,1433
TEST_DB_USER=sa
TEST_DB_PASSWORD=P@ssw0rd
TEST_DB_TRUST_SERVER_CERT=True
TEST_DB_ENCRYPT=False

# Notes:
# - TEST_DB_PASSWORD is REQUIRED
# - For local dev in VS Code dev containers, set these in .devcontainer/devcontainer.json
# - For CI/CD, set these as pipeline secrets/environment variables
# - Never commit actual passwords to source control
